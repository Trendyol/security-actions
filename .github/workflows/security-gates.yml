name: security-gates

on:
  workflow_call:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  security-gates:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set start time
        run: echo "START_TIME=$(date +%s)" >> $GITHUB_ENV


      - name: Run Gitleaks
        run: |
          wget -q https://github.com/gitleaks/gitleaks/releases/download/v8.22.1/gitleaks_8.22.1_linux_x64.tar.gz && tar -xzf gitleaks_8.22.1_linux_x64.tar.gz
          START=$(date +%s); ./gitleaks detect --source . --report-format sarif --report-path gitleaks.sarif --redact || true; 
          echo "Scan commit for alerting findings"
          ./gitleaks detect --source . --log-opts="--max-count=1 $GITHUB_SHA" --report-path commit-gitleaks.sarif --redact || true; END=$(date +%s)
          echo "GITLEAKS_DURATION=$((END - START))" >> $GITHUB_ENV

      - name: Run Semgrep Scan
        run: |
          pip install semgrep
          START=$(date +%s); semgrep scan --config="p/default" --sarif --output=semgrep.sarif || true; END=$(date +%s)
          echo "SEMGREP_DURATION=$((END - START))" >> $GITHUB_ENV

      - name: Run Grype SBOM scan
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin
          START=$(date +%s); grype dir:. -o sarif > grype.sarif || true; END=$(date +%s)
          echo "GRYPE_DURATION=$((END - START))" >> $GITHUB_ENV


      - name: Upload Results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always() && github.event.repository.private == false
        with:
          sarif_file: .
          checkout_path: ${{ github.workspace }}

      - name: Compute vulnerability counts
        id: vuln_count
        run: |
          safe_count() {
            if [ -f "$1" ] && [ -s "$1" ]; then
              # Check if it's an object with .runs, otherwise treat as 0
              jq 'if type == "object" and .runs then [.runs[].results[]?] | length else 0 end' "$1" || echo 0
            else
              echo 0
            fi
          }

          S_TOTAL=$(safe_count semgrep.sarif)
          G_CRIT=$(jq 'if type == "object" and .runs then [.runs[].results[]? | select(.properties.severity == "Critical")] | length else 0 end' grype.sarif || echo 0)
          
          # Process Gitleaks (Full Scan)
          LEAK_COUNT=$(safe_count gitleaks.sarif)
          echo "LEAKS_COUNT=$LEAK_COUNT" >> "$GITHUB_ENV"

          # Process Gitleaks (Current Commit)
          if [ -f commit-gitleaks.sarif ]; then
            CURRENT_LEAK_COUNT=$(safe_count commit-gitleaks.sarif)
            echo "CURRENT_LEAKS_COUNT=$CURRENT_LEAK_COUNT" >> "$GITHUB_ENV"
            
            echo "CURRENT_LEAKS_DETAILS<<DELIMITER" >> "$GITHUB_ENV"
            jq -r 'if type == "object" and .runs then .runs[].results[]? | "\(.locations[0].physicalLocation.artifactLocation.uri):\(.locations[0].physicalLocation.region.startLine)" else "No findings" end' commit-gitleaks.sarif >> "$GITHUB_ENV"
            echo "DELIMITER" >> "$GITHUB_ENV"
          else
            echo "CURRENT_LEAK_COUNT=0" >> "$GITHUB_ENV"
            echo "CURRENT_LEAKS_DETAILS=Commit'te Bulgu yok." >> "$GITHUB_ENV"
          fi

          echo "S_TOTAL=$S_TOTAL" >> "$GITHUB_ENV"
          echo "G_CRITICAL=$G_CRIT" >> "$GITHUB_ENV"
      
      - name: Send to Elastic
        env:
          CA_CERT: ${{ secrets.APPSEC_CA }}    
        run: |
          TOTAL_DURATION=$(( $(date +%s) - START_TIME ))
          echo "$CA_CERT" > trendyolrootca.crt

          PAYLOAD=$(jq -n \
            --arg timestamp "$(date --utc +'%Y-%m-%dT%H:%M:%SZ')" \
            --arg repo "${{ github.repository }}" \
            --arg leaks "$LEAKS_COUNT" \
            --arg s_total "$S_TOTAL" \
            --arg g_crit "$G_CRITICAL" \
            --arg total_dur "$TOTAL_DURATION" \
            '{
              "@timestamp": $timestamp,
              "repository": $repo,
              "vulnerability_summary": {
                "gitleaks": ($leaks | tonumber),
                "semgrep_total": ($s_total | tonumber),
                "grype_critical": ($g_crit | tonumber)
              },
              "total_duration_sec": ($total_dur | tonumber)
            }')

          curl -X POST "${{ secrets.APPSEC_ELASTIC_URL }}" \
            -u "${{ secrets.APPSEC_ELASTIC_USER }}:${{ secrets.APPSEC_ELASTIC_PASS }}" \
            --cacert trendyolrootca.crt -H "Content-Type: application/json" -d "$PAYLOAD"

      - name: Slack Notification
        env: 
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ env.CURRENT_LEAKS_COUNT != '0' ]; then
          MESSAGE=":rotating_light: *Son Commit'te Gitleaks Secret Bulguları Tespit Edildi! İnceleme Gerekli <!subteam^S094H6SEC06|appsec-oncall>*
          *Repo:* "https://github.com/${{ github.repository }}"
          Commit: "https://github.com/${{ github.repository }}/commit/${{ github.sha }}"
          Kullanıcı: "https://github.com/${{ github.actor }}"
          *Toplam Bulgu:* $CURRENT_LEAKS_COUNT

          *Bulguların Listesi:*
          \`\`\`
          $CURRENT_LEAKS_DETAILS
          \`\`\`"
          
          PAYLOAD=$(jq -n --arg msg "$MESSAGE" '{text: $msg}')
          
          curl -X POST -H 'Content-type: application/json' --data "$PAYLOAD" "$SLACK_WEBHOOK_URL"
          fi
